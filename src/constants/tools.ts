/**
 * Fixed MCP tool name for the channel's datasets-metadata tool. Unlike `sdmx_proxy`
 * (whose name is channel-configurable and arrives via the `query_data` tool's
 * structured content), this tool is always registered under this constant name.
 */
export const DATASETS_METADATA_APP_TOOL_NAME = 'datasets_metadata_app';
